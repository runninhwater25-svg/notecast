# Notecast 跨平台视觉系统

> 最后更新：2026-09-01
>
> 职责：UI Guide；产品行为见 `PRODUCT.md`，业务异常见 `docs/BUSINESS_FLOW.md`

## 方向

深色机场导向系统。每次任务是一条从录音到 Markdown 的路线，阶段编号和状态优先于装饰。

## 色彩

- 背景 `#111315`
- 表面 `#191C1F`
- 分隔线 `#30343A`
- 主文字 `#F2EFE6`
- 次级文字 `#B8B3A8`
- 路线黄 `#FFD000`
- 完成绿 `#55C76A`
- 错误红 `#F05252`

## 自适应原则

- 宽屏使用左侧导航和双栏编辑/预览；窄屏使用顶部上下文和单栏标签切换。
- 桌面控件支持鼠标与键盘；Android 主动作保持至少 48dp。
- 使用 Material 3 语义组件，品牌只通过色彩、阶段路线和排版层级表达。
- 不使用渐变、玻璃拟态、无意义卡片堆叠和装饰性动画。

## 输入站

- 录音、文件选择和桌面拖拽属于同一个入口，不拆成互相竞争的页面。
- 拖拽悬停时只强化边框与“松开即可导入”状态，不使用大面积动画。
- 文本和音频导入后必须明确显示文件名，以及下一步是“直接整理”还是“先转写”。
- Android 保留文件选择，不依赖桌面拖拽手势。

## AI 设置

- 先选择本地服务或在线 API，再呈现地址、模型、密钥和提示语。
- 本地预设是可编辑的起点，不把 Ollama 误称为语音转写服务。
- 密钥默认隐藏，说明保存位置；错误应靠近配置字段并给出修复方式。

## 状态与反馈

- 空状态明确给出录音、选择或拖拽入口，不以装饰代替下一步。
- 录音、转写、生成、编辑和导出使用文字状态，不只依赖颜色。
- 网络、权限、格式、额度和服务端错误提供具体原因及可恢复动作。
- 繁忙阶段禁用重复提交；失败不删除本地源文件或已编辑 Markdown。
